#include <backprop.hpp>
#include <vector>

/*
	Demonstrating the ability to load and save a model 
	(trained on a subset of f(x) = x^2 over [0, 100]
*/

std::vector<std::pair<int, int>> generateData() {

}